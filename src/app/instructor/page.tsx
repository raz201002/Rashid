import Link from "next/link";

export default function InstructorDemoPage() {
  return <main className="min-h-screen p-6 md:p-10"><div className="mx-auto max-w-4xl"><header className="flex flex-wrap items-center justify-between gap-4"><Link href="/" className="text-2xl font-black">GRIDWISE <span className="text-teal-700">STUDIO</span></Link><span className="rounded-full bg-amber-50 px-3 py-1 text-sm font-bold text-amber-900">Instructor demo · local learning state</span></header><h1 className="mt-12 text-4xl font-black">Instructor overview</h1><p className="mt-3 max-w-2xl text-slate-600">This initial view demonstrates how a facilitator can frame the learning sequence. It does not collect learner identities, submit results, or operate a production BESS.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{[["Suggested flow", "Load → sizing → dispatch → thermal → uncertainty → economics"], ["Discussion prompt", "Which constraint is binding: power, energy, temperature, or tariff economics?"], ["Evidence boundary", "Use this tool to learn and compare assumptions—not to approve a physical installation."]].map(([title, copy]) => <section key={title} className="rounded-2xl bg-white p-5 shadow-sm"><h2 className="font-bold">{title}</h2><p className="mt-2 text-sm text-slate-600">{copy}</p></section>)}</div></div></main>;
}
