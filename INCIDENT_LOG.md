# Incident Log

This is the durable engineering record for anything that can affect quality, project flow, or timeline. Every entry remains here after resolution so the team can learn from it. GitHub Issues remain the user-controlled case record; only the user closes a case.

| ID | Date | Category | Case | Impact | Root cause | Corrective action | Verification | Status |
|---|---|---|---|---|---|---|---|---|
| ENV-001 | 2026-09-03 | Environment | #1 | Local build work could not start. | The runtime Git installation lacked the HTTPS remote helper; Node/npm were not on the usable tool path. | Installed/repaired Node.js/npm and full Git HTTPS support. | `node --version`, `npm --version`, HTTPS clone, commit, and push succeeded. | Resolved |
| ENV-002 | 2026-09-03 | Environment | #1 | Dependency installation and repository commands could not write safely after cloning. | The installer and sandbox ran under different Windows identities, triggering Git ownership protection and folder-write restrictions. | Scoped Git safe-directory usage to the cloned repository and granted the workspace write access. | `npm install`, Git branch creation, commits, and pushes succeeded. | Resolved |
| QA-001 | 2026-09-04 | Code quality | #1 | Lint blocked the dispatch-chart update. | The session-restoration effect synchronously set state; a home navigation used a plain anchor. | Deferred restoration through a timer and used Next.js `Link`. | `npm run lint` passed. | Resolved |
| QA-002 | 2026-09-04 | Code quality | #1 | Production build blocked after chart work. | Recharts tooltip formatter declared a narrower value type than the library accepts. | Made the formatter safely handle an optional numeric value. | `npm run build` completed with exit code 0. | Resolved |
| CI-001 | 2026-09-04 | CI / verification | #1 | GitHub Actions failed: no tests were available to run; subsequent build was skipped. | A Python-oriented `lib/` ignore pattern unintentionally ignored `src/lib`, which contains the shared engine and Vitest test. | Anchored the ignore rule to `/lib/` and committed `src/lib` explicitly. | Local lint, test, and build passed; GitHub Actions runs 2–5 passed after commit `f6bf689`. | Resolved |
| QA-003 | 2026-09-04 | Tooling compatibility | #1 | Vitest warned that the current configuration would not be supported by a future native config loader. | The TypeScript Vitest configuration used ESM syntax while the package defaulted to CommonJS. | Marked the project package as ESM and made Vitest’s path setup ESM-safe. | `npm run test` passed with no compatibility warning. | Resolved |
| E2E-001 | 2026-09-04 | Test automation | #1 | Initial Playwright run did not return because spawned development-server processes remained active. | The browser test used the development server, whose Windows child-process lifecycle was unreliable in this environment. | Stopped only the test processes and changed E2E to build once, then test against a reusable production server process. | Playwright executed against the managed production server. | Resolved |
| E2E-002 | 2026-09-04 | Test automation | #1 | First route/reload assertions failed. | The test used non-exact text locators, each of which matched both a short label and its longer containing sentence. | Changed both route and post-reload assertions to exact accessible-text matches. | Route/reload E2E test passed. | Resolved |
| E2E-003 | 2026-09-04 | Test automation | #1 | Local terminal wrapper left temporary Playwright and Next server processes after a successful E2E run. | Windows child-process cleanup is unreliable in this managed terminal wrapper. | Stopped only the identified temporary test processes; CI uses an isolated runner. | `npm run test:e2e` completed its build and passed the route/reload test before cleanup. | Monitoring |
| QA-004 | 2026-09-04 | Test automation | #1 | Unit-test run failed after adding Playwright route tests. | Vitest’s default file pattern included the `e2e/*.spec.ts` Playwright suite. | Restricted Vitest discovery to `src/**/*.test.ts`. | `npm run test` passed with 4 unit tests. | Resolved |
| E2E-004 | 2026-09-04 | UI / test automation | #1 | Knowledge Check direct-route test failed after nine other route tests passed. | The page title and its inner panel had the same accessible heading text; an intermediate rerun used a stale production server. | Renamed the inner panel to “Check your understanding,” rebuilt, and restarted the test server. | All 10 direct-navigation and reload Playwright tests passed. | Resolved |
| TRACK-001 | 2026-09-05 | Project tracking | Cases 003-005 | Requested Case creation could not be published from the Codex in-app browser. | The browser’s GitHub session is signed out. | User must sign in to GitHub in the opened browser; then the three approved Cases can be created. | Pending authenticated GitHub session. | Open |
| BRAND-001 | 2026-09-05 | Brand asset | Case 005 (pending) | The requested fixed watermark cannot yet be implemented as a genuinely transparent logo. | The supplied logo is JPEG, a format without alpha transparency, and its visible canvas is black. | Obtain a transparent PNG or SVG, or explicitly authorize background removal from this JPEG before implementation. | Pending brand-asset decision. | Open |

## Entry rules

1. Log a discovered problem immediately, even when a fix is available.
2. Keep the entry after a fix; change only its status and verification evidence.
3. Use `Monitoring` until the relevant external verification (for example CI) succeeds.
4. Open or update the associated GitHub case when the problem changes scope, timeline, requirements, or needs a user decision. The user alone closes cases.
